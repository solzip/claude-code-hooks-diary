# DEPRECATED

This directory contains the v1.0 scripts. They are **no longer maintained**.

Use `src/claude_diary/` (v2.0+) instead:

```bash
pip install claude-diary
claude-diary init
```

These files are kept for historical reference only.
